package com.mycompany.javafxapplication1;

public class DB {
    // DB.java is now deprecated and no longer handles database operations.
    // All database-related functionality has been split into:
    // - DBConnection.java (Handles database connections)
    // - UserDB.java (Handles user management)
    // - SessionDB.java (Handles session storage)
    // - FileDB.java (Handles file metadata and encryption)
    // - LoadBalancerDB.java (Handles storage containers and load balancing)
}
